# Models = Database tables and key registrations. Tables represented in Python classes,
# call manage.py task and update migrations to update app database structure to match classes in this file

from django.db import models

# Create your models here.
